// Menu functions
    void ravenMenu(const char* options[], int numOptions, int selected); // Show menu with options
    void ravenDialog(const char* title, const char* message); // show dialog box with title and message
    void ravenProgressBar(int progress); // Show progress bar with current and max progress
    void ravenStatusBar(const char* status); // Show status bar at the bottom of the screen